export const MATCH_ERRORS = {
    MATCH_NOT_FOUND: "Match not found",
    MATCH_ID_REQUIRED: "Match ID is required",
    LOCATION_REQUIRED: "Both users must have locations set in their profiles to get suggestions",
} as const;
